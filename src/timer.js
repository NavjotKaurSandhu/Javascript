// SETTIMEOUT
const setTimeoutId = setTimeout(() => console.log('Set timeout example'), 500);

// SETINTERVAL
const setIntervalId = setInterval(() => console.log('Set interval example'), 500);

// CLEAR TIMEOUT
clearTimeout(setTimeoutId);

// CLEAR INTERVAL
clearInterval(setIntervalId);